fly.toml

# Git/CI metadata
.git
.github
.gitignore

# Python env and caches
.venv/
venv/
__pycache__/
*.py[cod]
*.pyi
*.pyo
*.pyd
.ipynb_checkpoints/

# Editor and OS cruft
.vscode/
.idea/
.DS_Store
*.swp

# Local env/config
.env
.env.*
.envrc

# Build artifacts
dist/
build/

# Data: include only artifacts + seeds needed at runtime.
# Re-include the directories themselves as well as their contents, otherwise
# some builders won't descend into an excluded dir to pick the files back up.
data/*
!data/artifacts
!data/artifacts/**
!data/seeds
!data/seeds/**
