music-rec/
  pyproject.toml            # or requirements.txt
  .env                      # SPOTIFY creds, runtime config
  README.md
  data/
    raw/                    # jsonl or csv (API responses, backups)
    processed/              # parquet dataset
    artifacts/              # scaler.pkl, features.npy, id_map.json, annoy.index, etc.
  src/
    recsys/
      __init__.py
      config.py             # paths, feature lists, constants
      io.py                 # read/write parquet, artifacts
      etl_spotify.py        # data acquisition
      preprocess.py         # cleaning + feature engineering
      recommenders/
        base.py             # Recommender interface
        cosine.py           # Dense cosine similarity
        annoy_index.py      # Optional ANN (Annoy/FAISS)
      service/
        api.py              # FastAPI app
        schemas.py          # Pydantic models
    cli/
      build_dataset.py      # orchestrates ETL -> parquet
      train_index.py        # fit scaler, build matrices/index
      sanity_checks.py      # quick QA reports
  web/                      # frontend (React or static)
    index.html
    app.js
